name: github_users_flutter
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.0+4
  build_runner: ^1.7.3
  json_serializable: ^3.2.5
  json_annotation: ^3.0.1
  date_format: ^1.0.8
  intl: ^0.16.1
  
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_launcher_icons: "^0.7.4"

flutter:
  assets:
    - assets/github-mark.png

  uses-material-design: true

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icone_app.png"